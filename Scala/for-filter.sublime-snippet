<snippet>
<content>
<![CDATA[
for (
	${1:element} <- ${2:collection}
	${3:if ${4: condition-1 using element};}
	) {
		$0	
	}
]]>
</content>
	<tabTrigger>for</tabTrigger>
	<!-- Optional: Set a scope to limit where the snippet will trigger -->
	<scope>source.scala</scope>
</snippet>