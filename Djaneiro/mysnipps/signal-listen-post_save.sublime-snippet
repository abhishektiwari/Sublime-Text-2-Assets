<snippet>
<content>
<![CDATA[
from django.db.models.signals import ${1:post_save}
from django.dispatch import receiver

@receiver($1, sender=${2:ModelName})
def $2_handler(sender, instance, created, **kwargs):
	${4:arg1} = kwargs['$4']
	${5:arg2} = kwargs['$5']
	# Do something
	$6
]]>
</content>
    <tabTrigger>signal</tabTrigger>
    <scope>source.python</scope>
    <description>signal listen post_save</description>
</snippet>
